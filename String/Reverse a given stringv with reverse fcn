Write a C++ program to reverse a given string using reverse function.

#include <iostream>
#include <string>

using namespace std;

int main() {
    string str = "zaved";
    reverse(str.begin(), str.end());
    cout << str;
    return 0;
}
