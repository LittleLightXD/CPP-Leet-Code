Write a C++ program to reverse a given string.

#include <iostream>
#include <string>

using namespace std;

int main() {
    string str = "zaved";
    reverse(str.begin(), str.end());
    cout << str;
    return 0;
}
